// 028 Inicio del proyecto - Sección 4
let base = 2;

for (let i = 1; i <= 10; i++) {
  const element = base * i ;
  console.log(`${base} * ${i} = ${element}`)
}
// Node nos permite grabar la tabla de multiplicar y crear un archivo de texto con ese valor. Esto se encuentra en archivo 029